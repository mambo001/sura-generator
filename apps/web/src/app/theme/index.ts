import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#FF885B',
    },
    secondary: {
      main: '#FFE5CF',
    },
    background: {
      default: '#557C56',
    },
    text: {
      primary: '#33372C',
    },
  },
});
